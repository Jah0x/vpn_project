{
  "name": "vpn-backend",
  "private": true,
  "scripts": {
    "build:server": "ts-node scripts/gen-openapi.ts && tsc -p tsconfig.json",
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5",
    "@types/node": "^18.17.0"
  }
}
